__pycache__/
*.pyc
data/.odds_api_key
# Large historical corpus for rho fitting — re-fetch via the curl in
# data/History/DATA_QUALITY.md (public CC0); keep the repo lean.
data/History/*.csv

# Playwright MCP scratch artifacts
.playwright-mcp/

# Raw futi.live screenshots (provenance for the 6/18 ratings; OCR'd into the CSV, ~19MB)
data/Ratings/Futi_6_18/

# football-data.co.uk closing-odds CSVs (re-fetch via backtest_market.py --download)
data/MarketHistory/
